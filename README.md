# Advanced File Finder

Windows-first, read-only desktop file search with advanced filters. It uses UV, a `src` layout, pytest, and Ruff.

The complete usage guide is added in the final documentation stage.